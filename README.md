# 🌌 AetherLens - 原生桌面漫画翻译客户端 (PyQt6 Apple 极简美学版)

## ✨ 软件特色

- **🍎 苹果极简美学原生 GUI**：
  - 基于 PyQt6 打造的现代 macOS 质感桌面端软件（深色/浅色主题、毛玻璃半透明顶栏、圆角卡片、柔和阴影、精致层级）。
  - 支持漫画图片与章节文件夹的**直接拖拽导入**。
  - **多元视口模式**：支持 **译图预览**、**双联对比 (Side-by-Side)**、**卷帘对比滑块 (Split Slider)**、**纯净擦除背景** 与 **原图** 快速切换。
  - 画布支持无极缩放、右键/中键平滑拖拽漫游。

- **🔍 本地高精度 OCR 识别 (GPU 加速)**：
  - 原生内嵌 **PaddleOCR 3.x** 与 **EasyOCR**，支持竖排/横排日语漫画对齐与智能段落聚合。
  - 自动检测并启用 NVIDIA GPU (CUDA) 硬件加速。

- **🧹 深度学习图像修复 (Simple-LaMa & OpenCV)**：
  - 深度集成 **LaMa (Simple-LaMa)** 图像补全大模型，全自动擦除对话框文字并还原漫画线条背景。
  - 配合自适应掩码膨胀与高保真双边羽化滤波，背景消除不留瑕疵。

- **🤖 多模型 AI 翻译管理器**：
  - 支持 **DeepSeek**、**OpenAI (GPT-4o/mini)**、**Google Gemini** 及任意 OpenAI 兼容的第三方 / 本地模型 API。
  - 针对漫画语境优化的本地化提示词系统，保留角色语气与拟声词韵味。

- **💬 可视化交互编辑 & 排版工作台**：
  - 画布上直观显示已识别的气泡框，**鼠标点击即可选中**。
  - 右侧检查器实时调整气泡原文/译文、字体、字号缩放、自适应字号、文字描边（Auto 对比描边 / 自定义）、气泡背景填充不透明度。
  - 支持实时重绘与一键导出高清 PNG / JPG 图像。

- **⚡ 批量处理与异步多线程**：
  - 所有耗时模型计算与网络请求在后台 `QThread` 线程池异步执行，UI 保持 60 FPS 流畅无卡顿。
  - 支持整个章节/多张漫画一键批量队列翻译与自动导出。

---

## 🚀 启动方式

### 方式 1：一键双击启动 (推荐)
直接双击运行项目根目录下的 **`start_desktop.bat`**。

### 方式 2：命令行启动
```bash
python run_desktop.py
```

---

## ⚙️ 软件配置指引

1. 打开软件后，点击右上角 **「⚙️ 设置」**。
2. 在 **「🤖 AI 翻译大模型」** 中填入您的 API Key（如 DeepSeek 或 OpenAI Key）以及模型名称。
3. 在 **「🔍 OCR 文字识别」** 中确认启用了 GPU 硬件加速。
4. 点击 **「保存配置」** 即可开始使用。
